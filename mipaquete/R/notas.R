# condicional if
#
# x = -4
#
# if (x > 0 ) {print("este número es positivo")
#   } else {print("este número es negativo")}
#
# ifelse(x > 0, print("este número es positivo"), print("este número es negativo"))
#
# x <- seq(-3,3)
#
# for (i in x) {
#   print(i)
#   if (i == 0) {print("este número es igual a cero")} else {
#     if (i > 0 ) {print("este número es positivo")
#     } else {print("este número es negativo")}
#   }
# }
#
# x = -3
# while (x < 0) {
#   x = x + 1
#   print(x)
# }
#
# library(data.table)
# seq_dna <- c("aaaattttgcttt")
# seq_rna <- c("acgacguuuuucccaaa")
#
# if (length(seq_dna[seq_dna %like% "u"]) > 0) { print(seq_dna)}
#
# if (length(seq_rna[seq_rna %like% "u"]) > 0) { print(seq_rna)}
#
#
#
#
#
#
#
#
#
#
#
